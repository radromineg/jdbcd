import Foundation

let numbers = [4, 2, 9, 1, 5, 6]

let sortedNumbers = numbers.sorted(by: { (first, second) -> Bool in
    return first < second
})

print(sortedNumbers) // 输出: [1, 2, 4, 5, 6, 9]
